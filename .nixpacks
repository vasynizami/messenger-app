[phases.setup]
nixPkgs = ["nodejs_22", "npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["ng build"]

[start]
cmd = "npx http-server dist/messenger-app/browser -p $PORT -a 0.0.0.0 -c-1" 